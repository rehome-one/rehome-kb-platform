fastapi==0.136.1
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
pyjwt[crypto]==2.12.1
httpx==0.28.0
cachetools==5.5.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
prometheus-client==0.21.1
# pgvector Python lib — SQLAlchemy types `Vector(N)` + asyncpg adapter.
# Server-side требует `pgvector/pgvector:pg16` Docker image (см. infra/
# docker-compose.yml). ADR-0010 #126.
pgvector==0.4.0
# Object storage SDK (ADR-0012). Lightweight S3-compatible client.
# Used только в documents/storage.py; lazy import чтобы boot не падал
# если MINIO_ENABLED=False (CI default).
minio==7.2.10
# Multipart parsing для UploadFile (ADR-0012 Phase B). FastAPI runtime
# requirement когда роутер использует File/Form. Pin 0.0.27 — фикс
# CVE-2026-24486 / -40347 / -42561 (DoS на malformed multipart).
python-multipart==0.0.27

