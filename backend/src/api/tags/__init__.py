"""Tags module — публичный список тегов с агрегацией article_count.

Источник истины tags — `Article.tags` JSONB column (E2.4 #34).
Отдельной нормализованной таблицы tags нет: данные дедублируются
агрегацией на чтение, а GIN-индекс на articles.tags ускоряет
существующие фильтры по тегам.
"""
