# Approved breaking changes for `oasdiff breaking` job.
#
# Каждая не-comment строка ниже — substring для матчинга в error message
# oasdiff. При совпадении breaking change игнорируется (CI зелёный).
#
# Workflow добавления записи:
#   1. Architect одобрил deviation в Issue/PR в формате CLAUDE.md §9:
#      `@architect approved deviation from <ТЗ>: <reason>`
#      или `approved spec evolution: <reason>`.
#   2. Добавить сюда строку с reference на Issue + comment-id.
#   3. Обновить yaml + open PR с обоими изменениями.
#
# Periodic audit (E5.x backlog): просмотр устаревших записей, restore-of-spec
# когда implementation подтянулась обратно к стандарту.

# ============================================================
# 2026-05-12 — ArticleInput.access_level становится required.
# Architect approve: Issue #27 issuecomment 4428692249 (E4.1, security-by-design).
# Spec sync: Issue #42 / PR #43 (этот PR).
# Real-world impact: zero — код enforce'ил это с E4.1; клиенты без
#   `access_level` уже получали 422. Спека догоняет реальность.
property access_level became required

# ============================================================
# 2026-05-16 — Documents download: убираем 501 в пользу 302 (ADR-0012).
# Architect approve: ADR-0012 (Принято 2026-05-15) + Issue #211 PR #211.
# Backend изменение: PR #212 (Phase A landed) — 501 deferral заменён
#   на реальный 302 + signed MinIO URL. Spec catch-up в этом PR.
# Real-world impact: zero — клиенты не строили логику на 501; download
#   был помечен `x-deferred-to-epic`.
response 501 removed
removed the success response with the status
